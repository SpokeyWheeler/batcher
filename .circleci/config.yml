# Golang CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-go/ for more details
version: 2.1
orbs:
  gor: hubci/goreleaser@1.0.0
  snyk: snyk/snyk@0.0.12
workflows:
  main:
    jobs:
      - build
      - test
      - gor/release:
          version: 0.147.1
          go-version: 1.15.5
          dry-run: true
  release:
    jobs:
      - test:
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^v\d+\.\d+\.\d+$/
      - gor/release:
          version: 0.147.1
          go-version: 1.15.5
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^v\d+\.\d+\.\d+$/
jobs:
  build:
    docker:
      # specify the version
      - image: cimg/go:1.15.5
        auth:
          username: $DOCKERUSER
          password: $DOCKERPASS

      - image: circleci/postgres:9.6-alpine-ram
        auth:
          username: $DOCKERUSER
          password: $DOCKERPASS
        environment:
          POSTGRES_USER: btest
          POSTGRES_DB: batchertestdb
          TEST_DATABASE_URL: postgresql://btest@localhost:5432/postgres
          PGDATA: /var/lib/postgresql/9.6/main

      - image: circleci/mysql:latest-ram
        auth:
          username: $DOCKERUSER
          password: $DOCKERPASS
        environment:
          MYSQL_ROOT_PASSWORD: btestroot
          MYSQL_DATABASE: batchertestdb
          MYSQL_USER: btest
          MYSQL_PASSWORD: btest

    working_directory: ~/go/src/github.com/SpokeyWheeler/batcher
    steps:
      - checkout
      - run: go get -v -t -d ./...
      - run: go build -i -v -ldflags="-X main.version=$(git describe --tags)" github.com/SpokeyWheeler/batcher
      - run: snyk/scan

  test:
    docker:
      - image: 'cimg/go:1.15.5'
        auth:
          username: $DOCKERUSER
          password: $DOCKERPASS

      - image: circleci/postgres:9.6-alpine-ram
        auth:
          username: $DOCKERUSER
          password: $DOCKERPASS
        environment:
          POSTGRES_USER: btest
          POSTGRES_DB: batchertestdb
          TEST_DATABASE_URL: postgresql://btest@localhost:5432/postgres
          PGDATA: /var/lib/postgresql/9.6/main

      - image: circleci/mysql:latest-ram
        auth:
          username: $DOCKERUSER
          password: $DOCKERPASS
        environment:
          MYSQL_ROOT_PASSWORD: btestroot
          MYSQL_DATABASE: batchertestdb
          MYSQL_USER: btest
          MYSQL_PASSWORD: btest

    steps:
      - checkout
      - run: go get -v -t -d ./...
      - run: go build -i -v -ldflags="-X main.version=$(git describe --tags)" github.com/SpokeyWheeler/batcher
      - run: sudo apt update -y
      - run: sudo apt install -y wget postgresql-client mysql-client
      - run: wget -qO- https://binaries.cockroachdb.com/cockroach-v20.2.3.linux-amd64.tgz | tar xvz
      - run: sudo cp -i cockroach-v20.2.3.linux-amd64/cockroach /usr/local/bin/
      - run: cd test; ./cockroach.sh; cd ..
      - run: cd test; ./postgres.sh; cd ..
      - run: cd test; ./mysql.sh; cd ..
      - save_cache:
          key: go-mod-v1
          paths:
            - /go/pkg/mod

